# Docker Example

Build the server for use in the centos7 Docker container

```
mvn clean package -Dos.detected.classifier=linux-x86_64-fedora
```

Launch the various containers using docker-compose

```
docker-compose up
```

This starts timely, grafana and a process to insert fake test data. It also mounts the timely grafana plugin for grafana.

Browse to the UIs:

[Grafana](https://localhost:3000) User: admin Password: admin

